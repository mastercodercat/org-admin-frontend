import * as fromExplorerFilters from './explorer-filters.actions';

xdescribe('filterExplorerFilterss', () => {
  it('should return an action', () => {
    // expect(fromExplorerFilters.filterExplorerFilterss().type).toBe('[ExplorerFilters] Filter ExplorerFilterss');
  });
});
