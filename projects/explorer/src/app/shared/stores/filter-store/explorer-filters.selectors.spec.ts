import * as fromExplorerFilters from './explorer-filters.reducer';
// import { selectExplorerFiltersState } from './explorer-filters.selectors';

xdescribe('ExplorerFilters Selectors', () => {
  it('should select the feature state', () => {
    // const result = selectExplorerFiltersState({
    //   [fromExplorerFilters.explorerFiltersFeatureKey]: {}
    // });
    //
    // expect(result).toEqual({});
  });
});
