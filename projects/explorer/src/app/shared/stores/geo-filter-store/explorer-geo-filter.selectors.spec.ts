import * as fromExplorerGeoFilter from './explorer-geo-filter.reducer';
// import { selectExplorerGeoFilterState } from './explorer-geo-filter.selectors';

xdescribe('ExplorerGeoFilter Selectors', () => {
  it('should select the feature state', () => {
    // const result = selectExplorerGeoFilterState({
    //   [fromExplorerGeoFilter.explorerGeoFilterFeatureKey]: {}
    // });

    // expect(result).toEqual({});
  });
});
